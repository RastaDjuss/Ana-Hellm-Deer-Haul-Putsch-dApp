{
  "name": "ana-chain",
  "version": "0.1.0",
  "description": "Ana-Chain fullstack application social media govenance dapp",
  "scripts": {
    "install": "pnpm recursive install --child-concurrency 1 --network-concurrency 4 --prefer-offline --frozen-lockfile",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --ext ts,tsx",
    "anchor:build": "cd anchor && anchor build",
    "docker-buildx": "docker buildx build ."
  },
  "dependencies": {
    "next": "13.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-next": "^13.3.0",
    "typescript": "^5.0.0",
    "tailwindcss": "^3.3.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
